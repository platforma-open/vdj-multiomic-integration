import { expect, test } from "vitest";

// Placeholder so the package is non-empty and the lint/check gate passes. The real
// @platforma-sdk/test blockTest suite (plan Task 7 — the Level-4 matrix + Level-5 cross-block e2e)
// lands once the aggregation workflow is implemented (Tasks 3-4) and a local backend is available.
test("placeholder", () => {
  expect(1 + 1).toBe(2);
});

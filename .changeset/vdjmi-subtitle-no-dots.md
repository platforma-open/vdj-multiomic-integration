---
"@platforma-open/milaboratories.vdj-multiomic-integration.ui": patch
---

Remove dots from the default block-label subtitle. The default is derived from the selected dataset's label; dots are now replaced with spaces so they no longer appear in the subtitle or the derived trace label. A subtitle the user types manually is left untouched.
